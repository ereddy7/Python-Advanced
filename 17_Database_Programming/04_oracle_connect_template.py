# Requires Oracle and oracledb. Replace placeholders.
# import oracledb
# con=oracledb.connect(user="USER",password="PASSWORD",dsn="localhost/XEPDB1")
# print(con.version); con.close()
print("Configure Oracle credentials, then uncomment the example.")
