# Requires mysql-connector-python and MySQL.
# import mysql.connector
# con=mysql.connector.connect(host="localhost",database="DB",user="USER",password="PASSWORD")
print("Configure MySQL credentials, then uncomment the template.")
